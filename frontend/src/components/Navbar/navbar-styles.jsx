import styled from 'styled-components'

const Nav = styled.nav`
    margin: 0;
    padding: 0;
    width: 100%;
    position: absolute;
    display: flex;
    justify-content: space-around;
    align-items: center;
    z-index: 2;
    `

export default Nav